# Python 3.11 runtime used for the new DiT training and evaluation runs.
# The Nimrod run used the CUDA 13.0 build of PyTorch.
torch==2.11.0
torchvision==0.26.0
timm==1.0.28
numpy==2.3.5
safetensors==0.8.0
Pillow==12.3.0
scipy==1.15.3
torch-fidelity==0.3.0
